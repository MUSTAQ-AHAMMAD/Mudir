apiVersion: 1
# Grafana datasource provisioning — auto-register Prometheus.
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
