# ORCHESTRA (Mudir) — test dependencies.
#
# The test-suite mocks every heavy backend (Ollama, whisper.cpp, the ML models
# and the WhatsApp API), so it deliberately does NOT require torch,
# transformers, sentence-transformers or chromadb. Only the lightweight
# dependencies that the code imports eagerly are installed here.
#
#     pip install -r requirements-test.txt
#     pytest
# ---------------------------------------------------------------------------

# Test runner + coverage.
pytest>=8.0,<9
pytest-cov>=5.0,<7

# Imported eagerly by the code under test.
requests>=2.32.2,<3          # llm_service / whisper_service HTTP client
urllib3>=2.7.0,<3
httpx>=0.27,<1               # WhatsApp WATI client

# Async database layer (integration tests; gated on a real PostgreSQL).
SQLAlchemy[asyncio]>=2.0.36,<3
asyncpg>=0.30.0,<1
